

### # Control Flow

- **if**: Checks a condition and executes a block of code if the condition is true.
- **else**: Provides an alternative block of code to execute if the `if` condition is false.
- **elif**: Stands for "else if"; allows checking multiple conditions sequentially.
- **while**: Runs a block of code repeatedly as long as a condition remains true.
- **for**: Iterates over a sequence (like a list or range) and executes a block of code for each item.
- **break**: Exits the current loop prematurely.
- **continue**: Skips the remaining code in the current iteration of a loop and moves to the next iteration.

### # Logical Operations

- **and**: Returns true if both operands are true.
- **or**: Returns true if at least one of the operands is true.
- **not**: Returns true if the operand is false, and false if the operand is true.

### # Data Structures

- **list**: An ordered, mutable collection of items, which can be of different types.
- **dict**: A collection of key-value pairs, where each key is unique.
- **set**: An unordered collection of unique items.
- **tuple**: An ordered, immutable collection of items, which can be of different types.

### # Miscellaneous

- **import**: Includes a module or specific components of a module in the current namespace.
- **from**: Used in conjunction with `import` to import specific parts of a module.
- **as**: Renames a module or component for easier reference.
- **with**: Simplifies exception handling and resource management (e.g., file operations) by ensuring cleanup after use.
- **pass**: A null statement used as a placeholder in loops, functions, classes, etc.
- **return**: Exits a function and optionally returns a value to the caller.
- **yield**: Pauses a function and returns a generator object, allowing the function to produce a series of values over time.
- **raise**: Generates an exception manually.
- **try**: Starts a block of code to test for exceptions.
- **except**: Catches and handles exceptions raised by the `try` block.
- **finally**: Executes a block of code regardless of whether an exception was raised or not.
- **is**: Tests for object identity, checking if two references point to the same object.
- **in**: Checks if a value is present in a sequence or collection.
- **def**: Defines a function.
- **class**: Defines a new class, a blueprint for creating objects.
- **lambda**: Creates a small anonymous function at runtime.
- **nonlocal**: Refers to variables in the nearest enclosing scope that is not global.
- **global**: Refers to variables in the global scope, allowing modifications.
- **del**: Deletes an object or an item from a collection.
- **assert**: Tests a condition and raises an `AssertionError` if the condition is false.
- **async**: Declares a function or method as asynchronous, enabling the use of `await` within it.
- **await**: Pauses execution until an asynchronous operation completes, typically used inside `async` functions.


# Arithmetic Operators

Addition (+): Adds two numbers.
result = 5 + 3  # result is 8

Subtraction (-): Subtracts the second number from the first.

result = 5 - 3  # result is 2

Multiplication (*): Multiplies two numbers.
result = 5 * 3  # result is 15

Division (/): Divides the first number by the second, resulting in a float.
result = 5 / 3  # result is approximately 1.6667

Floor Division (//): Divides the first number by the second and returns the integer part of the result.
result = 5 // 3  # result is 1

Modulus (%): Returns the remainder of the division.
result = 5 % 3  # result is 2

Exponentiation (**): Raises the first number to the power of the second number.
result = 5 ** 3  # result is 125